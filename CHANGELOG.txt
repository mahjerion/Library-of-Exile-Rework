v.1.6.5
- fix custom dimension gamerules so it respects original gamerules if its not overriding them
- overrides worldborder so it makes it do nothing
- added configs to disable worldborder and gamerule overrides if they're ever buggy in the future